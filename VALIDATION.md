# 验证记录

日期：2026-09-19。所有测试仅使用合成歌词和音频。

- 本机 Node.js 24.15.0，npm 11.12.1。
- 单元及集成测试：19 项通过，包含真实 Python 双向兼容测试。覆盖歌词配对与时间戳、offset、状态和历史、时间边界与重叠、项目备份校验、IndexedDB 并发写入、XLSX 合并范围与文本安全。
- 原 Python CLI：从原实现生成 XLSX，Web 导入并导出后由原实现读取，数据和合并范围完全一致；原提交为 `d61296eb31b01b692c8fd3a30ee42578a4e59dca`。
- 本机 Google Chrome：8 项端到端测试通过。覆盖完整编排、真实 WAV 播放、高亮、暂停/变速/定位/循环、刷新恢复、Excel/JSON 下载、LRC 与整体偏移、备份重新关联、非法文件保留原数据、短歌曲替换、手机无横向溢出、存储失败、多标签页冲突、项目切换。
- 桌面和手机：使用 `scripts/capture.mjs` 生成截图并检查控制台异常与页面溢出。
- WebKit：官方与备用下载地址均 TLS 连接失败，本机未能执行。CI 已配置 WebKit，但未推送远程，因此尚无 CI 运行结果。
- Safari 与 Microsoft Edge 产品浏览器：未做实际验证；不将 Chrome 或 WebKit 的结果冒充这些浏览器的结果。

TypeScript 检查、生产构建和格式检查通过；`npm audit` 为 0 漏洞。

生产构建产物位于 `dist/`；未部署站点，未创建远程仓库。

## 2026-09-20 播放器交互修正

- `npm run check`：13 项单元/集成测试通过，Python 专项默认跳过；类型检查与构建通过。
- `npm run format:check` 与 `git diff --check` 通过。
- Chrome Playwright：6 项通过。新增真实合成音频 A/B 回跳、反向区间拒绝、替换清除、播放跟随开关、精确出入点校验、缩放边缘拖动、重叠回退和撤销测试。
- 本轮未运行 WebKit，未验证 Safari。

## 2026-09-20 打点快捷键与拖动限制

- 删除出入点数值输入框，两个高亮打点按钮与 A/B 控件同排；A/B 与片段出入点分别使用 `A`、`B`、`[`、`]`，输入框及弹窗不触发快捷键。
- `npm run check`：14 项通过，类型检查及构建通过；格式与 diff 检查通过。
- Chrome E2E 6 项通过，覆盖快捷键、输入保护、缩放后两侧边缘限制和撤销；纯逻辑另覆盖乱序列表、整体平移与歌曲边界。
- 本轮未运行 WebKit 或真实 Safari。

## 2026-09-20 限制新建段落点击区域

- 新建事件仅绑定片段轨道空白层，并检查点击时间不属于已有段落；刻度、播放头、A/B 标记及轨道外区域不触发新建。
- `npm run check`：14 项通过、Python 专项默认跳过，类型检查及构建通过；格式与 diff 检查通过。
- Chrome E2E 7 项通过，新增非空白点击不弹窗、缩放滚动后空白点击定位测试。本轮未运行 WebKit。

## 2026-09-20 提示、时间轴摘要与自动出点

- Toast 固定浮动、6 秒自动关闭，不挤占工作区；类型、等大拍数和编辑图标同排；时间轴新增单行动作摘要及省略号。
- 按拍数设出点使用 `入点 + 拍数 × 60 / BPM`，通过原区间校验，不截断、不改变拍数。快捷键 `E`；A/B 循环切换快捷键 `L`。
- `npm run check`：14 项单元/集成测试通过，Python 专项默认跳过；类型检查、构建、格式检查通过。
- Chrome E2E：8 项通过，新增 toast 几何位置不变、同排标题、动作摘要样式、自动出点成功与重叠拒绝、循环快捷键验证。本轮未运行 WebKit。

## 2026-09-20 LRC 来源与歌词编辑工作流

- Toast 放大居中、播放头增加片段上方的时间标签且高于选中片段；撤销/重做移到页头。
- Project v2 新增可选 lyricSource（原文、文件名、解析模式和偏移），兼容未含字段的 v2 JSON。LRC 原文与偏移随本地项目及 JSON 保存，XLSX 不写入。
- 歌词采用双栏多行输入；已载入来源再次打开直接多选，可重新编辑原文，非法来源不覆盖已保存来源。同刻其余条目在选择后禁用。
- `npm run check`：15 项通过（Python 专项默认跳过），类型与构建通过；格式及 diff 检查通过。Chrome E2E 9 项通过，覆盖关闭/刷新恢复、双栏保存、失效来源保护、同刻禁选及播放头点击。本轮未运行 WebKit。

## 2026-09-21 播放头平滑更新

- 播放头从 80ms React 状态节流中分离，每个 animation frame 直接读取 audio.currentTime 更新位置；定位立即更新，编辑区仍节流，避免整页逐帧重绘。
- `npm run check`：15 项通过、Python 专项默认跳过；类型检查、构建、格式检查通过。
- Chrome E2E 10 项通过，新增连续帧位置采样、音频时钟误差、暂停后定位不漂移验证。本轮未运行 WebKit。

## 2026-09-21 新建片段出点

- 修复时间轴新建只写起点、导致进入未排时区域的问题。按拍数与 BPM 计算出点，限制到后一段起点或歌曲结束；创建弹窗展示实际区间及空隙不足提示，不移动已有片段。
- `npm run check`：16 项通过（Python 专项默认跳过），类型与构建通过。Chrome E2E 11 项通过，含空隙新建、刷新后保留位置。格式及 diff 检查通过；本轮未运行 WebKit。

## 编辑卡片自适应高度

- 编辑面板按实际标题高度分配剩余空间，动作区最小高度、内边距和间距随面板高度压缩。
- Chrome E2E 12 项通过；使用 32 拍合成片段验证 922×880、1282×720、922×720 下卡片无内部滚动，页面无横纵溢出，对时按钮在视口内。极长内容仍保留滚动以避免数据不可访问。
- 单元测试、类型检查、构建、格式与 diff 检查通过。本轮未运行 WebKit。

## 2026-09-21 舞者画布与全员队形关键帧

- 新增 Project v2 可选 `choreography`：全曲舞者名单、全员共享队形关键帧、归一化坐标、双手光棒颜色和登退场状态；旧 v2 JSON 缺少字段时按空队形处理，XLSX 不写入。
- Chrome E2E 16 项通过，覆盖双手 23 色色板、SVG 画布边界、拖动自动建帧、全员插值、瞬时换色、登退场、关键帧拖动/改时/删除/撤销、多人播放/变速/A-B、刷新恢复、冲突禁写、窄屏切换和既有工作台回归。
- 单元测试 20 项通过，覆盖插值、颜色、同刻帧、登退场、删除、ID/坐标/时间校验与 JSON 往返。
- WebKit 未执行：本机缺少 `/Users/dijkstra0x3/Library/Caches/ms-playwright/webkit-2359/pw_run.sh`，需要安装 Playwright WebKit 浏览器后才能运行；不能据此宣称 Safari 已验证。

## 缩放时间轴分页跟随与默认光棒

- 缩放大于 1 时，播放且开启“跟随播放”会在播放头接近可视区右侧时自动横向分页；暂停、手动定位或关闭跟随不强制滚动。
- 光棒色板新增“黑色（没拿棒）”，共 24 色；新舞者左右手默认使用“极橙”。

## 跟随播放状态修复

- 修复播放循环闭包使用初始缩放和跟随状态的问题。现在缩放时间轴、关闭/开启“跟随播放”后，播放循环立即读取最新状态。
- 新增 Chrome E2E：验证缩放后播放头分页、关闭跟随后停止滚动、重新开启后恢复跟随。

## 2026-09-21 自定义画布尺寸与双手同色

- 画布尺寸支持整数宽 320–4000、高 240–3000，默认 800×600；随项目与 JSON 保存，支持撤销。旧 JSON 解析补默认值，旧 IndexedDB 项目通过显示层回退兼容。
- 舞者坐标仍归一化保存；SVG 视口、网格、拖动坐标与逐帧渲染同步读取新尺寸。圆点按短边等比缩放，避免变成椭圆。
- 完成此前未提交的“左右手同时”按钮，选色通过一次队形更新同时写入双手。
- `npm run check`：21 项通过、Python 专项默认跳过；类型检查、构建与格式检查通过。
- Chrome 全套 20 项：19 项通过，包括新增尺寸校验、横竖比例、实时拖动、撤销重做、刷新、旧 IndexedDB 兼容、双手同色。既有“播放头逐帧跟随音频且暂停定位不漂移”在本机两次运行未达到 40 帧采样中超过 22 个不同位置的阈值（16/18）；未修改或放宽测试，不能报告全套通过。本轮未运行 WebKit。

## 2026-09-21 可选站位缩放

- 添加舞者与画布尺寸按钮相邻。尺寸弹窗默认勾选“按比例缩放舞者站位”；取消后全帧保留相对左上角的画布坐标，缩小越界时限制至既有归一化边界。
- `npm run check`：23 项单元/集成测试通过，Python 专项默认跳过，类型检查和构建通过；格式及 diff 检查通过。
- Chrome 队形专项 7 项通过，覆盖两种模式、按钮位置、全帧坐标、撤销及刷新。未重跑上一轮未通过的播放头帧率测试；未运行 WebKit。

## 2026-09-21 时间轴拖动收尾修复

- 段落块与队形关键帧共用指针会话：按 pointerId 接收窗口级 move/up/cancel，处理丢失捕获、窗口失焦、标签隐藏、项目/音频切换、只读状态及组件卸载。清理监听与释放捕获先于保存，结束幂等。
- 移动时发现左键已松开，提交最后有效预览并结束；丢失捕获或失焦取消预览。拖动段落前暂停音频，避免自动跟随同时滚动。
- `npm run check`：23 项单元/集成测试通过（Python 专项默认跳过），类型检查及构建通过；格式及 diff 检查通过。
- Chrome 相关回归 9 项通过，含新增捕获丢失、失焦、取消、窗口级松开、无按键移动不再拖动和一次撤销恢复。不宣称全量浏览器回归通过；此前帧率采样问题及 WebKit 本轮未验证。

## 2026-09-21 画布中心坐标保留

- “按比例缩放舞者站位”首次及每次打开尺寸弹窗均默认不勾选；未勾选按 `新归一化坐标 = 0.5 + (原归一化坐标 - 0.5) × 原长宽 / 新长宽` 转换所有关键帧，保留相对画布中心的偏移。缩小越界仍限制至边界；勾选时继续保留归一化坐标。
- Chrome 尺寸相关测试 2 项通过，覆盖默认状态、两种模式、坐标、撤销和刷新；单元测试包含非等比调整与双向中心偏移检查。类型、构建、格式及 diff 检查通过。本轮未运行全套 E2E 或 WebKit。

## 2026-09-21 限位后在其他块上松手

- 复现了捕获提前丢失导致限位预览回退的问题：正常松手通过，但提前释放捕获时原实现将拖动取消。
- 调整共享拖动会话：`lostpointercapture` 只代表事件目标改变，元素仍在文档时继续使用窗口级事件完成拖动；真正取消、窗口失焦、元素移除等仍回退。无按键移动仍结束会话，避免黏拖。
- `npm run check`：24 项单元/集成测试通过（Python 专项默认跳过），类型、构建、格式及 diff 检查通过。
- Chrome 相关回归 6 项通过，包括限位后在前块上松手、捕获提前丢失后保存、关键帧保存、单次撤销、防黏拖及既有边界校验。本轮未运行全套 E2E 和 WebKit。

## 2026-09-21 直接拖动播放头

- 播放头竖线增加命中范围，竖线与时间标签均可拖动定位；保留抓取偏移，缩放和横向滚动后按实际轨道尺寸计算。拖动前暂停，范围限定到歌曲首尾；松手保存播放位置，不新增编排撤销记录。
- 复用指针会话处理捕获丢失、松手、取消及失焦；滑动预览不逐次触发项目保存。未载入歌曲不可拖动。
- `npm run check`：24 项单元/集成测试通过（Python 专项默认跳过），类型检查、构建、格式与 diff 检查通过。
- Chrome 相关回归 6 项通过：标签/竖线拖动、缩放滚动定位、边界、暂停、失焦、无撤销记录及段落/关键帧事件隔离。本轮未运行全套 E2E 或 WebKit。

## 2026-09-21 队形标题栏与姓名气泡层级

- 舞者选择、绿色添加图标、红色全曲删除图标、重命名、入场、退场、画布尺寸和“显示名字”统一到队形标题栏；添加/删除使用图标按钮并保留 aria-label 与 title。
- 姓名气泡作为独立 SVG 顶层渲染，高于所有舞者圆点；显示开关不进入编排撤销历史，气泡不拦截拖动和点击。
- Chrome 队形专项 9 项通过，覆盖标题栏同排、小屏无溢出、姓名显示/重命名/入退场和既有队形流程。本轮未运行 WebKit。

## 2026-09-21 LRC 页头按钮文案

- 页头不再显示 LRC 文件名；已载入来源显示“编辑歌词”，未载入时显示“载入歌词”，点击行为保持不变。偏移按钮继续显示当前偏移。
- LRC 原文与偏移 Chrome E2E 通过；单元、构建、格式及 diff 检查通过。

## 2026-09-21 圆点点击、拖动收尾与色板精简

- 圆点首次点击只改变选中状态；已选圆点再次点击打开颜色编辑。颜色弹窗可直接重命名舞者。
- 圆点拖动改用全局指针会话，处理松手、失焦、取消、捕获丢失及组件卸载；拖动预览只在正常结束时保存。
- 色板删除有对应“极色”的基础色，只保留极色版本及黑色、特殊色；当前面板 16 色。旧 JSON 中的基础色仍通过兼容别名映射到对应极色。
- Chrome 队形专项 10 项通过；单元、类型、构建和格式检查通过。本轮未运行 WebKit。

## 2026-09-21 空格播放暂停

- 空格与播放按钮复用同一播放/暂停逻辑；阻止长按重复切换及触发焦点按钮的默认点击。输入框、文本域、选择框、可编辑内容、弹窗、输入法组合及修饰键下不触发。
- `npm run check`：24 项通过、Python 专项默认跳过，类型及构建通过；格式与 diff 检查通过。Chrome 相关测试 3 项通过，覆盖空格、未载入、长按、输入与弹窗保护、循环及播放跟随。本轮未运行全套 E2E 或 WebKit。

## 2026-09-21 JSON 导出后立即导入

- 合成复现：IndexedDB 旧草稿仍带 schemaVersion 1，经新版 UI 编辑后原样导出，随即被只接受 v2 的导入器拒绝。导出现在使用当前格式标记并执行导入同款校验；不更改原草稿内容或在失败时替换项目。未知/旧格式外部文件仍不自动更改语义，提示回到原项目重新导出。
- 结构错误现在报告具体字段路径，JSON 语法错误独立提示，支持 UTF-8 BOM。新增旧草稿往返、完整队形/音频元信息/歌词来源保留、旧颜色与缺尺寸兼容、非法字段拒绝测试。
- 本轮 `npm run check` 27 项通过（Python 专项默认跳过），类型与构建通过。已编写隔离浏览器的实际下载/导入测试，但 Chrome 启动审批服务返回 503，未能执行；当前用户页面读取也被同一审批服务拒绝，尚未核实其具体文件是否命中此原因。未运行 WebKit。

### 后续验证补充

审批服务恢复后，Chrome 隔离合成测试已通过“旧本地草稿导出文件后立即导入保留队形”，确认实际下载的 JSON 使用 v2 并可立即导入。并未读取用户当前项目文件。

## 2026-09-21 一次性光棒累计消耗

- 按全员关键帧分别追踪左右手，首次取棒、换色及退场再入场计新棒；同色连续关键帧不计，黑色不计。旧基础色统一归到对应极色。全曲统计包含已保存的越界帧并明确提示。
- 导出弹窗显示每色总量、全曲总数、逐人用量和毫秒级换色顺序。Excel 末尾新增“光棒用量统计”页，保持首张六列编排及合并结构不变，不把统计页当编排重新导入。
- `npm run check`：31 项通过（Python 专项默认跳过），类型与构建通过；随后真实 Python 双向兼容专项 1 项通过，包含带用量附表的文件。格式及 diff 检查通过。
- Chrome 相关测试通过 JSON 下载后立即导入、LRC 往返和统计页下载再导入；用量单元测试覆盖换回旧色、双手同色、隐藏帧、重新入场、重复时间状态、乱序帧、同名舞者、黑色/空数据及非法颜色。Excel 测试检查数值、切换时间、文本公式安全和原编排往返。本轮未运行全量 E2E 或 WebKit。

## 2026-09-21 控件焦点下的空格播放

- 修复 blanket input/select 过滤导致复选框、滑块和下拉框焦点截获空格的问题。空格使用捕获阶段处理，并阻止 keydown/keyup 默认激活；保留文字/数值编辑、弹窗和输入法保护。其余打点快捷键的输入保护不变。
- `npm run check`：31 项通过、Python 专项默认跳过，类型与构建通过；格式及 diff 检查通过。
- Chrome 相关测试 3 项通过：按钮焦点与长按、复选框保持选中状态、滑块/下拉值不变、文字空格输入、弹窗保护及原打点快捷键。本轮未运行全套 E2E 或 WebKit。

## 2026-09-21 色板内重命名返回流程

- 重命名入口移到颜色弹窗标题行；从色板进入重命名后，保存、关闭或 Escape 均返回原色板，保留手部模式和冻结时间。画布工具栏的独立重命名仍按原流程关闭。
- Chrome 相关测试 3 项通过，覆盖保存回退、取消保留旧名、空名禁止保存、手部模式保留、继续换色及独立工具栏重命名。`npm run check` 31 项通过（Python 专项默认跳过），类型/构建/格式及 diff 检查通过。本轮未运行全套 E2E 或 WebKit。

## 2026-09-21 紧凑用量与双色切换序列

- 导出弹窗移除用量表格和舞者折叠表格：用量横向单行，每位舞者一行，时间戳配与画布一致的左右半圆色点；长序列行内滚动，退场标记保留。
- 鼠标悬停、触摸点击和键盘聚焦显示左右颜色及新增根数；提示层挂在当前 dialog 内，避免滚动容器裁剪。点击外部、滚动、失焦或 Escape 隐藏提示。
- Chrome 相关测试 2 项通过，含触摸/悬停/键盘、手机不溢出、实际截图检查和 Excel 统计页下载再导入。Excel 统计格式及累计消耗算法未改。`npm run check` 31 项通过（Python 专项默认跳过），类型、构建、格式及 diff 检查通过。本轮未运行全套 E2E 或 WebKit。

## 2026-09-21 同表简化换棒列表

- UI 标题改为“换棒列表”，仅展示新增光棒记录；双手未持棒、退场和纯丢棒记录隐藏，单手未持棒半圆不画黑色。累计消耗算法不变。
- Excel 只保留一张工作表：原六列编排下方追加单行用量汇总和每人一行时间戳＋双色圆点。序列嵌入本地生成的 PNG，姓名单元格批注含文字明细，不再额外建表。
- 附录文字只占 A 列，图片覆盖空白区域，Web 用命名区域标识附录；真实 Python 读取保持编排数据和原区域合并结构一致，新增合并只在附录。Python openpyxl 给出 DrawingML 支持不完整警告，故兼容验证仅承诺编排数据读取，不承诺原 Python 重导出保留附录图片。
- `npm run check`：33 项通过（Python 专项默认跳过），类型、构建、格式及 diff 检查通过；单独真实 Python 双向兼容 1 项通过。
- Chrome 相关测试 3 项通过，覆盖同表统计、图片数量、下载后重新导入、黑色过滤、触摸/悬停。另检查生成的 PNG 示例，时间戳与双色半圆显示正常。本轮未运行全套 E2E 或 WebKit。

## 2026-09-21 Docker 与 CI

- 新增多阶段 `Dockerfile`、只读 Nginx 运行容器、`compose.yaml` 和 GitHub Actions 容器构建、启动 smoke test 与 GHCR 发布 job。
- `docker compose config -q`、Compose JSON 展开和 GitHub Actions YAML 解析已通过；`npm run check`、`npm run format:check` 与 `git diff --check` 通过。
- 本机 Docker 镜像构建未能执行：沙箱无法访问 Docker socket，提权审批服务返回 503；未将构建失败误报为镜像验证通过。

## 2026-09-21 satintin 生产部署

- 新增 `.github/workflows/deploy.yml`：`main` 的 CI 成功后在 GitHub Actions 构建 `dist/`，通过 SSH 上传到 `/opt/wota-arrangement-web`，按提交号保留最近 5 个版本并原子切换 `current`；健康检查失败恢复上一版本。
- 初次发布已完成：版本 `3eeec5e` 位于 `/opt/wota-arrangement-web/releases/3eeec5e`。`https://wota.satintin.com/healthz` 返回 `ok`，首页返回 `<title>Wota · 编排工作台</title>`，HTTP 自动 301 跳转 HTTPS。
- 宿主机 Nginx 独立站点配置通过 `nginx -t`。Let's Encrypt 独立证书仅包含 `wota.satintin.com`，有效期至 2026-12-20；`/etc/cron.d/certbot` 每日执行续期并在成功后重载 Nginx。
- `certbot renew --dry-run` 在模拟旧的 `satintin.com` 多域名证书续期时长时间无结果，已人工中止；不记录为续期模拟通过。新站点证书的实际签发、安装和 HTTPS 握手均已验证。
- 服务器直接 Docker 构建未完成：访问 Docker Hub `registry-1.docker.io` 超时，未进入源码构建。生产 CD 因此传输静态产物，不依赖服务器访问镜像仓库；Dockerfile、根目录 Compose、CI 镜像构建和 GHCR 发布仍保留。
- 已创建公开仓库 `https://github.com/Spartan859/Wota-Arrangement-Web`，本地 `origin` 指向该仓库；`production` Environment 已配置 `DEPLOY_SSH_KEY` 和 `DEPLOY_KNOWN_HOSTS`（仅核对名称与存在性，不记录内容）。
- 生产发布使用无 sudo、无 Docker 权限的 `wota-deploy` 账号，仅拥有 `/opt/wota-arrangement-web`。GitHub Actions Deploy run `35620182161` 首次验证了构建、SSH 上传、原子切换和 HTTPS 检查。

## 2026-09-22 GitHub CI/CD 与生产修复

- 首次 CI run `35619249427` 未通过：Chromium 有 1 项刷新前未等待 IndexedDB 自动保存的测试错误；WebKit 在 Ubuntu runner 中无法让合成 WAV 进入可播放状态，造成音频依赖测试连锁失败。刷新测试已改为等待“已保存”，并按产品行为断言刷新后撤销历史为空。
- Vite 开发服务器曾在首次按需载入 `exceljs` 时返回 `504 Outdated Optimize Dep`，导致 Excel 下载 E2E 等不到下载事件；将 `exceljs` 加入 `optimizeDeps.include` 后，该专项测试在原 45 秒限制内用 1.8 秒通过。
- CI run `35622376631` 总结论为成功：Web checks（含 Chromium 40/40）、Python XLSX 兼容、容器构建与只读容器 smoke test、GHCR 发布全部通过。WebKit 兼容任务独立完整运行并上传 artifact `10650363158`，结果为 7 通过、33 失败；失败共同集中在 Ubuntu WebKit 无法加载测试合成 WAV，故当前不声明 WebKit/Safari 兼容通过，也不让这一环境限制阻断 Chromium 验证后的生产发布。
- 自动 Deploy run `35623039675` 成功发布提交 `086e93904338fe37acc1844518b24bd7649fa417`。服务器 `current` 已指向同一 SHA，`https://wota.satintin.com/healthz` 返回 `ok`，首页包含 `Wota · 编排工作台`。
- 生产曾因工作流执行 `install -d -m 700 /opt/wota-arrangement-web` 下线：Nginx worker 实际为 `nobody:nogroup`，无法穿越该目录，首页返回 404，而独立 `/healthz` 仍返回成功。已将目录权限改为 `0711`，并让发布脚本同时通过 HTTPS 验证健康端点和首页标题；首页不可读时会触发回滚。修复后再次确认 Nginx、HTTPS 首页、健康检查和发布 SHA。
